# `docs/ops/` — Operations runbooks (archive)

Canonical step-by-step runbooks live next to the code (e.g. `infra/keycloak-banxe-emi/RUNBOOK.md`). This directory holds **archived ops summaries** for handoff / on-call / regulator review — derived from source RUNBOOKs.

## Index

| File | Topic |
|---|---|
| `runbook-strategy-b-keycloak-2026-05-04.md` | Keycloak realm `banxe-emi` cutover via Legion (STRATEGY-B). |

## When to add an entry

After a significant production state change (cutover, migration, decommission), copy the relevant RUNBOOK section here and add ops-side context (network, on-call, backout one-pager). Reference the canonical RUNBOOK as source-of-truth.
