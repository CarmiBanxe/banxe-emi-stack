---
entry_id: banking-engine-b6-close
timestamp_utc: "2026-07-13T01:43:20Z"
sprint: B-6
status: CLOSED
branch: agent/factory/bankingengine/b6-memory
merged_commit: f47cf35
pr: 303
predecessor_entry: banking-engine-b5-open
---

# Banking Engine B-6 — Compliance Memory Sandbox (CLOSED)

## Objective

Deploy a sandboxed Graphiti/Neo4j compliance-memory sidecar for the Banking Engine,
wired to the 12 seed documents prepared in B-5 (MiCA, FCA recordkeeping, HITL thresholds,
retention schedule, internal policy). Fix all Semgrep CI findings (8 blocking) in the
Docker Compose manifest.

## Summary

| Item | Result |
|------|--------|
| Scope | Sandbox only — no live regulatory parsing, no real customer data |
| Semgrep findings | 8 → 0 (no-new-privileges + writable-filesystem-service, all 4 services) |
| Docker security | `read_only: true` + `no-new-privileges:true` + service-specific `tmpfs` mounts |
| pip in read-only | `--target /tmp/packages` + `PYTHONPATH=/tmp/packages` (graphiti + seed-loader) |
| Neo4j PID fix | `/var/run/neo4j` added to tmpfs (PID file path on container image) |
| Redis PID fix | `/run` added to tmpfs (redis-alpine PID file path) |
| PR status | Merged → main (f47cf35) via PR #303 |

## Artifacts

| Artifact | Path |
|----------|------|
| Docker Compose | `infra/docker/docker-compose.compliance-memory.yml` |
| Seed documents 1–7 | `seed/compliance-memory/MiCA-CASP-SANDBOX-v1.md` … (B-5 artefacts) |
| Seed documents 8–12 | `seed/compliance-memory/{BANXE-COMPLIANCE-POLICY,BANXE-HITL-THRESHOLDS,BANXE-RETENTION-SCHEDULE}-SANDBOX-v1.{md,yaml}` |

## Compliance Notes

- **SANDBOX ONLY** — no live Graphiti → Neo4j ingestion pipeline active.
- All thresholds in seed docs marked **PLACEHOLDER** pending MLRO/CRO sign-off.
- No real customer data, PII, or credentials in any artefact (I-02, I-24).
- `read_only: true` + `no-new-privileges:true` satisfies Semgrep policy
  `yaml.docker-compose.security.*` (CIS Docker Benchmark).
- ADR-103: Legion is thin-client only; no write path into banking memory from Legion.

## Next Sprint

**B-7 — Full Banking Sandbox Validation (FINAL sprint)**

E2E validation: payment intent → LangGraph stub → HITL gate → PENDING.
All seven compliance domains exercised: E2E, ISO, HITL, AUT, AUD, EU-AI-Act-Art.14, DLP.
