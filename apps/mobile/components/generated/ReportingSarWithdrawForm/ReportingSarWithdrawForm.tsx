/**
 * AUTO-GENERATED by proto-sync.py — DO NOT EDIT MANUALLY
 * Source: POST /v1/reporting/sar/{sar_id}/withdraw
 * IL-PROTO-01 | banxe-emi-stack
 */
import { View, Text, StyleSheet, ScrollView, TouchableOpacity } from "react-native";
import { TOKENS } from "../../../tokens";

interface ReportingSarWithdrawFormProps {
  onSuccess?: () => void;
  onError?: (error: Error) => void;
}

export function ReportingSarWithdrawForm({ onSuccess, onError }: ReportingSarWithdrawFormProps): React.ReactElement {
  return (
    <View
      style={styles.container}
      accessible
      accessibilityRole="none"
      accessibilityLabel="MLRO withdraws SAR (JMLSG §6.7)"
    >
      <Text style={styles.title}>MLRO withdraws SAR (JMLSG §6.7)</Text>
      <Text style={styles.route}>POST /v1/reporting/sar/{sar_id}/withdraw</Text>
      <View style={styles.disclosure}>
        <Text style={styles.disclosureText}>Auto-generated prototype</Text>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: TOKENS.bgCard,
    borderWidth: 1,
    borderColor: TOKENS.borderDefault,
    borderRadius: TOKENS.radiusXl,
    padding: TOKENS.spacing6,
    gap: TOKENS.spacing3,
  },
  title: {
    fontSize: 16,
    fontWeight: "600",
    color: TOKENS.textSecondary,
  },
  route: {
    fontSize: 11,
    fontVariant: ["tabular-nums"],
    color: TOKENS.textMuted,
    fontFamily: "monospace",
  },
  disclosure: {
    borderTopWidth: 1,
    borderTopColor: TOKENS.borderDefault,
    paddingTop: TOKENS.spacing3,
    marginTop: TOKENS.spacing1,
  },
  disclosureText: {
    fontSize: 11,
    color: TOKENS.textMuted,
  },
});

export default ReportingSarWithdrawForm;
