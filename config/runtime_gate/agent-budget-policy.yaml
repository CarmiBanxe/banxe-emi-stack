# RED-agent budget policy — config-as-data (ADR-030 §9), operator-owned.
# Money is Decimal (quoted strings, never float). Over-budget ⇒ the agent refuses.
# Fail-closed: no config, or no entry for an agent ⇒ that agent is refused.
schema: agent-budget-policy/v1
window: 24h                      # default rolling window
agents:
  audit_trail:                   # RED — see agents/passports/audit_trail/AGENT.md (status: PROPOSED; supersedes parked PRs #283/#284/#285 — #284 alone is a stale reference)
    max_tokens_window: 200000
    max_cost_window: "5.00"      # GBP, Decimal
    window: 24h
  banxe_aml_orchestrator:        # RED (future AML activation) — RESERVED: no agent passport/soul file exists yet (audited 2026-07-18, fleet governance audit). Pre-provisioned budget for a planned agent; do not remove without confirming a passport/soul file has landed under this name.
    max_tokens_window: 500000
    max_cost_window: "20.00"
    window: 24h
  beneficiary:                   # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 500000
    max_cost_window: "20.00"
    window: 24h
  fraud_tracer:                  # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 500000
    max_cost_window: "20.00"
    window: 24h
  ato_prevention:                # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 500000
    max_cost_window: "20.00"
    window: 24h
  midaz_mcp:                     # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 500000
    max_cost_window: "20.00"
    window: 24h
  scheduled_payments:            # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 300000
    max_cost_window: "10.00"
    window: 24h
  disputes:                      # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 200000
    max_cost_window: "10.00"
    window: 24h
  reconciliation:                # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 200000
    max_cost_window: "10.00"
    window: 24h
  fatca_crs:                     # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 200000
    max_cost_window: "5.00"
    window: 24h
  lending:                       # RED (operator 2026-07-13, PROPOSED)
    max_tokens_window: 200000
    max_cost_window: "5.00"
    window: 24h
